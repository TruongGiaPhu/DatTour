import React from 'react'

import { BarChart, Bar, Cell, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';



function ChartC({ data, name, pv }) {
    return (
        <div>

        </div>
    )
}

export default ChartC